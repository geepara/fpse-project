(* module A = Abi
module C = Contract *)

let () = print_string "hello"
  (* let abi_file_name = "exampleABI.json" in *)
  (* basically take the json file and convert to yojson so that fields can be accessed *)
  (* let abi: A.t = A.abi_of_json abi_file_name in
  let contract = Contract.
  print_string "abi_filename" *)
